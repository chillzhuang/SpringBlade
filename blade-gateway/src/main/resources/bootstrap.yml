server:
  port: 80

spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
    loadbalancer:
      retry:
        enabled: true


